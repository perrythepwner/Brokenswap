import React from 'react';
import { SupportedProviders } from '../constants';
import { Maybe } from '../types';
interface Props {
    selectedProvider: Maybe<SupportedProviders>;
    onClose: () => void;
    onBack: () => void;
    tray: React.ReactElement;
    content: Maybe<React.ReactElement>;
}
export default function ModalContainer({ onBack, onClose, selectedProvider, tray, content, }: Props): JSX.Element;
export {};
//# sourceMappingURL=modal-container.d.ts.map