import { CeloProviderProps } from './react-celo-provider-props';
import { Actions, ActionsMap, ReducerState } from './react-celo-reducer';
import { Network } from './types';
export type Dispatcher = <Type extends Actions['type'], Payload extends ActionsMap[Type]>(type: Type, ...payload: Payload extends undefined ? [undefined?] : [Payload]) => void;
type CeloStateProps = Pick<CeloProviderProps, 'dapp' | 'theme' | 'network' | 'defaultNetwork' | 'networks' | 'feeCurrency' | 'manualNetworkMode'> & {
    networks: Network[];
};
export declare function useCeloState({ dapp, network, defaultNetwork, manualNetworkMode, theme, networks, feeCurrency, }: CeloStateProps): [ReducerState, Dispatcher];
export {};
//# sourceMappingURL=react-celo-provider-state.d.ts.map