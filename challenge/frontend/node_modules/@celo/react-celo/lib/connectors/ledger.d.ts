import { CeloTokenContract } from '@celo/contractkit/lib/base';
import { MiniContractKit } from '@celo/contractkit/lib/mini-kit';
import { WalletTypes } from '../constants';
import { Connector, Network } from '../types';
import { AbstractConnector, updateFeeCurrency } from './common';
export default class LedgerConnector extends AbstractConnector implements Connector {
    private network;
    private index;
    feeCurrency: CeloTokenContract;
    initialised: boolean;
    type: WalletTypes;
    kit: MiniContractKit;
    private wallet;
    constructor(network: Network, index: number, feeCurrency: CeloTokenContract);
    private getWallet;
    private createWallet;
    private createKit;
    initialise(): Promise<this>;
    supportsFeeCurrency(): boolean;
    startNetworkChangeFromApp(network: Network): Promise<void>;
    updateFeeCurrency: typeof updateFeeCurrency;
    close(): void;
}
//# sourceMappingURL=ledger.d.ts.map