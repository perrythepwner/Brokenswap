import { ReactElement } from 'react';
import { Maybe } from '../types';
interface Footer {
    url: string;
    CTA?: string;
}
interface NamedFooter extends Footer {
    name: string;
}
interface FooterWithDescription extends Footer {
    name?: never;
    desc: string;
}
interface Props {
    title: string | ReactElement;
    content: ReactElement;
    wcUri?: Maybe<string>;
    footer?: NamedFooter | FooterWithDescription;
}
export default function ConnectorScreen({ title, content, footer }: Props): JSX.Element;
export {};
//# sourceMappingURL=connector-screen.d.ts.map