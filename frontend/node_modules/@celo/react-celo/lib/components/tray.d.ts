import React from 'react';
import { SupportedProviders } from '../constants';
import useProviders from '../hooks/use-providers';
import { Maybe } from '../types';
interface Props {
    selectedProvider: Maybe<SupportedProviders>;
    providers: ReturnType<typeof useProviders>;
    title: string | React.ReactElement;
    onClickProvider: (providerKey: SupportedProviders) => void;
    search?: string;
    onSearch?: (search: string) => void;
}
export default function Tray({ providers: providersByPriority, title, onClickProvider, selectedProvider, search, onSearch, }: Props): JSX.Element;
export {};
//# sourceMappingURL=tray.d.ts.map