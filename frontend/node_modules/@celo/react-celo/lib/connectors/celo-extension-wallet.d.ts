import { CeloTokenContract } from '@celo/contractkit/lib/base';
import { MiniContractKit } from '@celo/contractkit/lib/mini-kit';
import { WalletTypes } from '../constants';
import { Connector, Network } from '../types';
import { AbstractConnector } from './common';
export default class CeloExtensionWalletConnector extends AbstractConnector implements Connector {
    private network;
    feeCurrency: CeloTokenContract;
    initialised: boolean;
    type: WalletTypes;
    kit: MiniContractKit;
    constructor(network: Network, feeCurrency: CeloTokenContract);
    initialise(): Promise<this>;
    continueNetworkUpdateFromWallet(network: Network): void;
    startNetworkChangeFromApp(): void;
    supportsFeeCurrency(): boolean;
    private newKit;
    close(): void;
}
//# sourceMappingURL=celo-extension-wallet.d.ts.map