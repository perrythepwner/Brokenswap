import { CeloTokenContract } from '@celo/contractkit/lib/base';
import { MiniContractKit } from '@celo/contractkit/lib/mini-kit';
import { WalletTypes } from '../constants';
import { Connector, Dapp, Network } from '../types';
import { AbstractConnector } from './common';
export default class CoinbaseWalletConnector extends AbstractConnector implements Connector {
    private network;
    private manualNetworkingMode;
    initialised: boolean;
    type: WalletTypes;
    kit: MiniContractKit;
    feeCurrency: CeloTokenContract;
    private provider;
    constructor(network: Network, manualNetworkingMode: boolean, dapp: Pick<Dapp, 'name' | 'icon'>);
    initialise(): Promise<this>;
    private onChainChanged;
    private removeListeners;
    private newKit;
    private onAccountsChanged;
    supportsFeeCurrency(): boolean;
    startNetworkChangeFromApp(network: Network): Promise<void>;
    continueNetworkUpdateFromWallet(network: Network): void;
    close(): void;
}
//# sourceMappingURL=coinbase-wallet.d.ts.map